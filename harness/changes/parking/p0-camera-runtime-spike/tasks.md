# Tasks — P0 camera + runtime spike

Atomic, ordered. Each task names its writer and its verification evidence.

| # | Task | Files owned | Writer | Status | Verification evidence |
|---|---|---|---|---|---|
| 0 | Free disk space (user approval) and approve dependency downloads (PyPI env, ORT AAR, OpenCV Android, ML Kit) | — | user + Claude | partial: disk freed by user (12 GB free); PyPI env installed (310 MB, `tools/ocr-lab/.venv`); ORT AAR, OpenCV, ML Kit come via Gradle in task 7 | `df -h` 2.1 GB → 12 GB; pip list |
| 1 | Verify device + models: SoC/RAM/API via `adb`, model SHA-256 | `evidence/device-and-models.md` | Claude | done (2026-09-19) | `getprop`: SM-S928B / SM8650 / API 36 / 8 cores / ~11.3 GB; `shasum -a 256` matches `model-manifest.md` |
| 2 | Python env (uv, Python 3.12) and inspect both ONNX graphs (I/O names, shapes, opset) | `tools/ocr-lab/**` | Claude (agy had no write permission in headless mode) | done (2026-09-19) | `evidence/onnx-inspect.txt`: I/O `input[batch,1,64,64]`→`logits[batch,39]`, opset 17, 20/66 nodes, batch 1/8/64 run; matches manifest |
| 3 | Port reference preprocessing (fix grayscale input) and produce golden logits for fixture cells (FP32 + INT8) | `tools/ocr-lab/**` | Claude | done for synthetic cells with the authors' exact preprocessing (2026-09-19); handwriting golden pending task 5 / `realdataset` access | AC1: `tools/ocr-lab/fixtures/` (117 cells, `golden.json`, `golden_inp64.bin`), `evidence/golden-generation.txt`: FP32/INT8 argmax agreement 1.000; **finding: class order differs from guide** (`evidence/class-order-finding.md`) |
| 4 | Fixture sheet generator: A4, ArUco 0–3, QR signature, per-question markers, 10 mm cells from a bundle fixture; documented layout assumption | `tools/sheet-gen/**` | Claude | done (2026-09-19) | `tools/sheet-gen/` (generator, README with layout, committed `fixtures/`); PDF verified by rasterising and running the reference pipeline: `evidence/desktop-e2e.txt` — 4/4 markers found on clean and two degraded (perspective, blur, uneven light, noise) renders, 7/8 answers read exactly, the eighth is the deliberate ӨСТӘЛТӘН error read as written; gap recorded (contract-gaps 15). Desktop only, printed synthetic letters, timings not device evidence. Geometry then re-verified on the backend's real `blank.pdf` (`evidence/real-blank-check.txt`): marker positions match to 0.1 mm, 4 rows × 8 cells, QR read; blank sheet false positives 1/336 cell-trials (one `Г` in the name field on a degraded render) |
| 5 | Print sheets and hand-fill an evaluation set (own handwriting; consent/data decision recorded) | `tools/ocr-lab/fixtures-private/**` (ignored) | user + Claude | pending | set size and provenance recorded in evidence |
| 6 | Define `OcrEngine` port and `SheetEvidence` types (+ unit tests) | `src/ports/ocr-engine.ts`, `src/domain/ocr/**` | Claude | done (2026-09-19) | `src/ports/ocr-engine.ts`, `src/domain/ocr/evidence.ts`; `pnpm test --runInBand`: 2 suites, 17 tests passed; typecheck and lint clean |
| 7 | Expo module skeleton `modules/ocr-native` with ORT Android; load models from app-private dir via `adb push` | `modules/ocr-native/**`, `src/adapters/ocr-native/**`, `src/features/ocr-lab/**`, `app/ocr-lab.tsx`, `android/gradle.properties` (generated; arm64-v8a only, 4 GB heap) | Claude | done (2026-09-19) | Dev client built (`BUILD SUCCESSFUL in 2m 33s`) and installed on SM-S928B; `info()` on device: OpenCV 5.0.0 loaded, arm64-v8a (`evidence/device-selftest.md`) |
| 8 | Kotlin preprocessing + batch inference; parity test vs golden logits | `modules/ocr-native/**` | Claude | done for synthetic cells (2026-09-19); handwriting parity pending evaluation set | AC2: 117/117 cells pixel-exact (max diff 0); logits vs golden ≤1.3e-5 (FP32 XNNPACK) and ≤5.7e-6 (INT8 CPU); argmax 117/117 (`evidence/device-selftest.md`) |
| 9 | Runtime benchmark on device: FP32+XNNPACK vs dynamic INT8 CPU, cold/warm, per-stage timers | `modules/ocr-native/**`, `src/features/ocr-lab/**`, `app/ocr-lab.tsx` | Claude | done (2026-09-19) | AC3: `evidence/benchmark.md` (+ raw JSON): 72-cell sheet, 30 warm runs × 15 configs, two sweeps. Best FP32+XNNPACK 6 threads ≈110 ms median / ≤128 ms p95; INT8 CPU best ≈148 ms; preprocessing ≈26 ms warm; model+preprocessing ≈140 ms per sheet |
| 10 | Geometry + QR: OpenCV ArUco, homography, marker refinement, cell crops, ML Kit QR, quality gate | `modules/ocr-native/**` | Claude | partial (2026-09-19): rectify, cell location, classification and QR (OpenCV + ML Kit) done and measured on stills; marker-based fallback for a hidden corner marker and the frame-quality gate pending | AC4 (stills): `evidence/sheets-device.md` — 6/7 images fully correct (8/8 rows, name, QR), warm pipeline ≈ 570 ms per 10 MP sheet; fails with a hidden corner marker (authors' fallback → direct scaling); live-frame success rate needs task 11 |
| 11 | Camera lane A: CameraX inside module (frame → native pipeline, no JS pixels) | `modules/ocr-native/**` | Claude | pending | AC5 numbers, cold/warm |
| 12 | Camera lane B: VisionCamera v5 Nitro plugin, same measurements (only if feasible on SDK 57) | `modules/ocr-native/**` (alt lane), `package.json` | Claude | pending | AC6 comparison table or recorded infeasibility |
| 13 | Decision logic: `p(expected)`, margin, blank pre-filter, TTA on uncertain cells; evaluate on task 5 set | `src/domain/ocr/**`, `tools/ocr-lab/**` | Claude | partial: code + unit tests done (`decision.ts`, 14 tests incl. ӨСТӘЛТӘН case, confusable pairs, blanks); TTA and evaluation pending | AC8 report: false-accept, review rate, per-pair behaviour (needs task 5 data) |
| 14 | Zero-photo audit + soak: fs/net/log evidence, repeated-scan memory | `evidence/**` | Claude | pending | AC7: commands + outputs |
| 15 | Docs: ADR 0005 (+ camera ADR), ocr-pipeline, model-manifest, contract-gaps, STATUS, CLAUDE.md | see plan | Claude | partial: contract-gaps items 15–16, STATUS, model-manifest on-disk check done; ADR 0005/camera ADR/ocr-pipeline wait for measurements | AC10 + static gates: lint, typecheck, tests, check-deps, doctor |
| 16 | Independent review | `reviews/review.md` | reviewer other than implementers | pending | verdict recorded |

## Completion definition

- [ ] Every acceptance criterion in spec.md has evidence above.
- [ ] No open question was silently guessed.
- [ ] Review in `reviews/review.md` recorded.
