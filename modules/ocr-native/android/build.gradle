plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'expo.modules.ocrnative'
version = '0.1.0'

android {
  namespace "expo.modules.ocrnative"
  defaultConfig {
    versionCode 1
    versionName "0.1.0"
  }

  // Ship the OCR models (repo root models/ocr, SHA-256 in SHA256SUMS) inside the APK.
  sourceSets {
    main {
      assets.srcDirs += file("${projectDir}/../../../models/ocr")
    }
  }
}

dependencies {
  implementation 'com.microsoft.onnxruntime:onnxruntime-android:1.30.0'
  implementation 'org.opencv:opencv:5.0.0'
  implementation 'com.google.mlkit:barcode-scanning:17.3.0'
  implementation 'androidx.camera:camera-core:1.6.2'
  implementation 'androidx.camera:camera-camera2:1.6.2'
  implementation 'androidx.camera:camera-lifecycle:1.6.2'
  implementation 'androidx.camera:camera-view:1.6.2'
}
